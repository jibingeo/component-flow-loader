{
  "name": "dataflow-diagnostics-loader",
  "version": "0.0.0",
  "description": "A webpack loader for transforming component ASTs to render out diagnostic information",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gurdas Nijor",
  "license": "ISC",
  "dependencies": {
    "falafel": "^0.3.1",
    "lodash": "^2.4.1"
  }
}
